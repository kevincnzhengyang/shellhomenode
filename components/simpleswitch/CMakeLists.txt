# Edit following two lines to set component requirements (see docs)
set(COMPONENT_REQUIRES shnode)

set(COMPONENT_SRCS "src/simple_switch.c")
set(COMPONENT_ADD_INCLUDEDIRS "include")

register_component()
